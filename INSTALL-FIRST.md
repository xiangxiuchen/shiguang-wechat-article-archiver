# 拾光存档 0.4.0 开源测试版｜安装说明

这是一个 Chrome / Edge 通用的浏览器扩展，Windows 和 Mac 的安装步骤相同。它会把你当前打开的一篇公开公众号文章保存成单个离线网页。

## 安装前先知道

- 这是供朋友小范围试用的“解压安装版”，暂未上架浏览器商店。
- 请使用 Chrome 123+ 或 Edge 123+，以支持安全打开已完成的下载文件。
- 基础保存功能不调用 AI，模型调用 0 次，Token 消耗 0。
- 文章只在你的浏览器里整理，不会上传给开发者。
- 请只用于个人学习和资料备份；转载或再发布前请获得原作者授权。

## Chrome 安装

1. 解压收到的 ZIP 文件，不要直接在压缩包里打开；请放到固定文件夹，安装后不要移动或删除。
2. 在地址栏输入 `chrome://extensions/` 并回车。
3. 打开页面右上角的“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择解压后的 `shiguang-archive-extension` 文件夹；里面应该能直接看到 `manifest.json`。
6. 在浏览器右上角的扩展菜单中，把“拾光存档”固定到工具栏。

## Edge 安装

1. 解压收到的 ZIP 文件，放到固定文件夹；安装后不要移动或删除。
2. 在地址栏输入 `edge://extensions/` 并回车。
3. 打开“开发人员模式”。
4. 点击“加载解压缩的扩展”。
5. 选择解压后的 `shiguang-archive-extension` 文件夹。
6. 把“拾光存档”固定到工具栏。

## 保存文章

1. 打开一篇 `mp.weixin.qq.com` 的公开文章。
2. 点击浏览器右上角的“拾光存档”。
3. 确认标题后点击“保存当前文章”。
4. 第一次保存含图片文章时，浏览器会询问是否允许读取微信文章图片域名；这项权限只用于把原图放进离线文件。拒绝时仍可保存正文，图片位置会改成本地提示。
5. 等待界面显示“已保存”，然后在下载文件夹中打开 HTML 文件。

## 常见问题

**会不会消耗我的模型额度或 Token？**
不会。0.4.0 的基础保存链路没有模型接口，也没有 AI SDK。

**为什么浏览器提示“开发者模式扩展”？**
因为这是尚未上架商店的测试版。请只从可信的原始分享入口领取，并核对分享页提供的 SHA-256。

**为什么有些图片没有保存？**
图片可能发生重定向、格式不受支持、解码失败、尺寸或体积超限，或暂时无法访问。正文仍会保存，缺图位置会显示本地提示，不会偷偷回连远程图片。

**遇到问题如何反馈？**
普通问题可提交 GitHub Issue，或通过收到安装包的原分享渠道反馈。优先提供版本、操作系统、浏览器版本、错误编号和失败阶段。不要默认发送文章正文、完整链接、Cookie 或账号信息。详细说明见 `SUPPORT.md` 和 `SECURITY.md`。

**如何卸载？**
回到浏览器扩展管理页，在“拾光存档”卡片上点击“移除”。已经下载到本机的文章不会被自动删除。

拾光存档是独立开发者工具，与微信及腾讯不存在隶属、合作或官方背书关系。
