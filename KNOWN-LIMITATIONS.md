# 已知限制

1. 微信页面结构变化可能导致暂时无法识别文章。
2. 非 `mmbiz.qpic.cn` 来源的图片不会被抓取。
3. SVG、Canvas、CSS 背景图和部分复杂布局不能以原视觉形式离线保留；检测到时应显示“部分成功”和内容损失摘要。
4. HTML 伪装图片、无法实际解码、像素、动图帧数或累计解码量超限，以及发生重定向的图片会被拒绝。
5. 音视频、小程序、投票、直播、评论和其他互动内容只保留本地文字提示。
6. 超过 25,000 个正文节点、2 MiB 清洗后正文、100 张图片、单图 6 MiB、图片合计 24 MiB、整篇图片累计 1.2 亿像素／帧像素或最终文件 40 MiB 时会安全停止或部分保存。
7. 这版不提供批量抓取、定时任务、云同步、AI 摘要或标签分析。
8. macOS Chrome 的真实文章人工矩阵，以及 macOS Edge、Windows Chrome、Windows Edge 实机均尚未验收。
9. Beta 版同一时间只运行一个保存任务；请等待当前文章完成或取消后，再保存下一篇。
10. 浏览器完全退出、扩展被重新加载或系统关机后，未完成任务不会自动恢复。
