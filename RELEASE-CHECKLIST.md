# 发布前验收清单

规则：自动化测试、系统浏览器验证和真实文章人工验收是三类不同证据；只有实际完成的项目才能勾选。`npm run release` 通过不代表 Windows、Edge 或真实微信文章已经通过。

## P0：朋友测试包发出前

### 自动化与最终成品

- [x] `npm test` 全部通过：URL、图片结构/尺寸、单图帧像素、整篇累计解码预算、任务状态、内容损失、最小权限、0 AI/遥测及版本一致性。
- [x] `npm run test:browser` 全部通过，且测试来源为 `dist/shiguang-archive-extension`，不是 `dev` 演示页。
- [x] 最终 dist 完成完整成功、部分成功、取消、单任务锁和弹窗无报错测试。
- [x] 表单、模板等特殊结构被移除时记录 `dropped-markup`，且脱离正文树的图片不进入下载清单。
- [x] 极端动图和累计像素达到上限时停止在途及后续图片请求，成品明确进入部分保存。
- [x] 生成的 HTML 在浏览器离线模式打开，除本地 `file:` 外网络请求为 0。
- [x] `npm run release` 一次完成测试、构建、两种 ZIP、解包复测及 SHA-256。
- [ ] 朋友测试版 ZIP 根目录精确包含 `00-先看这里-安装导航.html` 与 `shiguang-archive-extension/`；后者根目录直接包含 `manifest.json`。
- [ ] 安装导航页已由最终朋友 ZIP 解压后双击验收：Chrome / Edge 切换、复制地址、Mac / Windows、固定图标、首次图片权限、使用、更新和卸载均可读。
- [ ] 安装导航页为单文件离线页面，不包含远程脚本、外链资源、联网请求或统计追踪。
- [ ] 最终 ZIP 弹窗显示当前 manifest 版本；成功与部分成功均可直接打开已完成存档，打开失败会定位文件夹。
- [ ] “复制安全反馈”仅包含白名单诊断字段；剪贴板拒绝时显示可手动复制的文本，且不泄露文章、链接、文件名或任务标识。
- [x] Chrome 商店版 ZIP 根目录直接包含 `manifest.json`。
- [x] 相同 dist 重复打包得到完全相同的 ZIP 哈希。

### macOS Chrome 真实文章人工验收

- [ ] 使用最终“朋友测试版 ZIP”全新解压安装，而不是源码目录。
- [ ] 普通图文、长图文、纯图片、GIF、表格、背景图各至少 1 篇。
- [ ] 音视频、小程序或互动文章明确显示部分成功，不显示完整成功。
- [ ] 图片权限允许：可下载图片，断网后仍可查看。
- [ ] 图片权限拒绝：正文仍保存，图片使用本地占位，结果为部分成功。
- [ ] 图片伪 MIME、损坏、超尺寸、超像素、超动图帧及超时均有明确失败原因。
- [ ] 同名重复保存不覆盖旧文件。
- [ ] 保存过程中关闭并重开弹窗，可看到当前任务；完成后可看到最近结果。
- [ ] 用户取消显示取消；任务总超时显示超时；两者不会混淆，也不会留下错误成品。
- [ ] 断网打开最终 HTML，开发者工具确认网络请求为 0。

### 安全与产品说明

- [x] 伪装域名和 HTTP 链接被自动化策略测试拒绝。
- [ ] 非文章页和微信登录页使用最终 ZIP 人工复核，确认不会生成误导性成品。
- [x] 最终安装包内没有模型/统计 SDK、API Key、遥测端点或远程脚本。
- [x] 隐私、权限、版权边界、非官方声明、卸载和安全反馈说明均可从安装包查阅。
- [ ] 测试者明确知道这是 Beta，复杂排版可能变化，内容损失会在结果中提示。
- [ ] 已确定公开支持入口；当前“通过原分享渠道反馈”只适用于小范围测试。

## P1：Windows 与 Edge 跨平台声明前

- [ ] Chrome / Windows：使用最终 ZIP 完成与 macOS Chrome 相同的真实文章矩阵。
- [ ] Edge / Windows：使用最终 ZIP 完成与 macOS Chrome 相同的真实文章矩阵。
- [ ] Edge / macOS：使用最终 ZIP 完成与 macOS Chrome 相同的真实文章矩阵。
- [ ] Windows 保留文件名、超长标题、中文路径、重复文件和下载拦截行为正常。
- [ ] Chrome 和 Edge 的权限允许、拒绝及扩展更新/重装行为均已记录。
- [ ] 至少一台普通配置 Windows 设备完成 100 张图片压力测试，无崩溃或明显失去响应。

## P2：浏览器商店公开发布前

- [ ] Chrome Web Store 开发者账号、商店资料、截图、图标、分类与支持网址齐备。
- [ ] Chrome Web Store 隐私披露与实际权限、网络行为一致。
- [ ] Microsoft Edge Add-ons 单独完成账号、资料、审核及支持入口。
- [ ] 正式版本有 Git 提交、版本标签、变更记录和可回滚的上一安装包。
- [ ] 从公开下载入口重新下载 ZIP，核对 SHA-256 后完成一次安装和保存验收。
- [x] 源代码已采用 MPL-2.0，代码、文章内容和项目标识的权利边界已写入安装包。

## 开源仓库首次公开前

- [x] `package-lock.json` 精确锁定开发依赖，干净环境可执行 `npm ci`。
- [x] `CONTRIBUTING.md`、`SECURITY.md`、`NOTICE.md`、`TRADEMARKS.md` 和 Issue 模板齐备。
- [x] `dist/`、`release/`、`node_modules/`、历史 ZIP 和本机缓存不进入源码版本控制。
- [x] GitHub Actions 覆盖 Node 20 的 macOS、Windows、Linux 基础与浏览器测试，并单独复核可重复发布流程。
- [ ] 建立独立 Git 仓库，确认提交身份，完成首个提交与 `v0.4.0-beta.1` 标签。
- [ ] 启用 GitHub Private Vulnerability Reporting，并把仓库、支持和隐私网址写回商店资料。
- [ ] 从全新公开克隆执行 `npm ci`、`npm test`、`npm run test:browser` 与 `npm run release`。

## 尚未验证的平台（不得写成已支持）

- [ ] iPhone / Safari / iOS Share Extension。
- [ ] Android 微信分享、Chrome、系统文件选择器及主流厂商真机。
- [ ] HarmonyOS NEXT / 华为应用市场。

这些平台由各自移动端项目单独验收，桌面扩展构建或 PWA 页面通过不能替代其安装与真机证据。
