# 参与贡献

欢迎提交可复现问题、小范围修复和与“用户主动保存当前公开公众号文章”直接相关的改进。

## 本地开发

需要 Node.js 20 或更高版本。

```bash
npm ci
npm test
npm run test:browser
npm run build
```

`npm run release` 会执行完整测试、生成朋友测试包与 Chrome Web Store 包，并复核成品哈希。发布包由维护者生成；普通 Pull Request 不应提交 `dist/`、`release/`、ZIP 或本机缓存文件。

## 贡献边界

1. 保持用户主动、单篇、公开文章、本机处理的产品边界。
2. 不加入绕过登录、付费、访问控制、频率限制或技术保护措施的能力。
3. 不加入 Cookie、聊天记录、公众号后台、浏览历史或账号凭证采集。
4. 不加入远程代码、文章上传、遥测、广告、模型调用或默认联网分析。
5. 图片和跳转必须继续执行精确域名、HTTPS、大小、结构、像素和总预算验证。
6. 无法完整保存的视觉、媒体或互动内容必须记录损失并显示“部分保存”，不得误报完整成功。
7. 新功能应继续符合 Chrome Web Store 的单一用途和最小权限要求。

## 测试与样本

- 修复问题时请增加最小、确定、可复现的测试。
- 测试夹具必须是原创合成内容或具有兼容授权的材料。
- 不要提交真实文章全文、未脱敏截图、完整文章链接、Cookie、账号、访问令牌、本地路径或个人信息。
- 涉及 Mac、Windows、Chrome 或 Edge 的兼容性结论，应说明是自动化、模拟环境还是真实设备证据。

## Pull Request

较大功能请先开 Issue 对齐范围。Pull Request 应说明：问题、修改范围、安全影响、测试证据以及是否改变权限、网络范围、文件格式或内容损失语义。

提交贡献表示你确认相关代码和素材是原创，或你拥有以 MPL-2.0 兼容方式提供它们的权利。
