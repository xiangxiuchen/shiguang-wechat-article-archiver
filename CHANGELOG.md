# 更新记录

## 0.4.0 · 2026-08-21

- 源代码改用 Mozilla Public License 2.0，明确代码、项目标识和用户保存内容之间的权利边界。
- 新增贡献指南、安全政策、问题模板和跨 Mac / Windows / Linux 的 GitHub Actions 测试流程。
- 声明并锁定 Playwright 开发依赖，外部贡献者可从干净克隆复现构建与浏览器测试。
- 将 `dist`、发布 ZIP、本机缓存和历史安装包排除在源码版本控制之外；发布成品继续由可重复发布流程生成。
- 保持单篇、公开文章、本机处理、0 模型、0 遥测和内容损失透明的产品边界。

## 0.3.2 · 2026-08-21

- 新增首次安装导航，用浏览器分流、进度检查和首篇测试降低朋友安装门槛。
- 保存完成后可直接打开离线 HTML；无法打开时安全回退到在文件夹中定位。
- 弹窗显示当前版本，并可一键复制不含文章内容、链接或账号信息的安全反馈。
- 在首次图片授权弹窗出现前增加用途说明，拒绝时仍可保存正文。

## 0.3.1 · 2026-08-21

- 增加单图帧像素、整篇累计像素和累计帧像素预算，阻止小体积极端大图或动图造成异常内存占用。
- 文章达到图片累计预算后停止后续图片请求与解码，剩余位置使用本地占位并明确显示部分保存。
- 表单、模板、无脚本区、选择框和文本框等特殊结构被安全移除时会记录内容损失，不再误报完整成功。
- 删除特殊结构后不再处理已脱离正文树的子节点，避免孤立图片进入下载清单。
- 新增极端动图、整篇累计像素、预算原子预留和特殊结构正文丢失回归测试。

## 0.3.0 · 2026-08-20

- 修复复杂内容被移除后仍显示完整成功的问题；输出内容损失清单并强制进入部分成功。
- 支持纯图片文章；SVG、Canvas、CSS 背景图和被清理的自定义组件会生成安全占位与明确提示。
- 图片从文件签名检查升级为结构、实际解码、尺寸、像素和动图帧数验证。
- 区分用户取消、任务总超时、单图超时和浏览器中断，不再把自动超时误报为用户取消。
- 拒绝微信图片域名权限时降级为正文存档，图片使用本地占位，不中断整个任务。
- 弹窗可以恢复全局进行中任务，以及当前文章最近的成功、部分成功或失败结果。
- 新增单一 `npm run release` 流程：测试、构建、朋友版和商店版打包、解包复测、SHA-256 一次完成。
- 商店 ZIP 的 `manifest.json` 位于根目录；朋友 ZIP 保留清晰的安装文件夹。
- 构建和 ZIP 改为可重复生成，并验证相同 dist 得到相同哈希。
- 浏览器完整链路测试改为从最终 dist 启动，覆盖完整成功、部分成功、取消、单任务锁和断网打开。
- 增加版本一致性门禁、测试版授权、安全反馈说明及平台实机验收边界。

## 0.2.1 · 2026-08-20

> 已撤回，不应继续分发。该版本尚未完整识别视觉内容损失，也没有完成最终成品的发布门禁。

- 浏览器的真实下载状态成为下载阶段唯一终态：完成则成功，确认中断后才显示取消。
- 修复取消与下载完成同时发生时，界面状态和下载目录可能不一致的问题。
- 下载交接增加 15 秒超时；失败会回收 Blob、释放单任务锁，并清理迟到创建的下载。
- 取消处理中禁用重复取消，避免同一任务被多次终结。
- 修正朋友解压 ZIP 后的 Chrome / Edge 安装路径。
- 增加取消竞态、终态优先级、交接超时和取消文件残留测试。

## 0.2.0 · 2026-08-20

- 修复保存完上一篇后，新文章仍显示旧结果的问题。
- Beta 版限制为同一时间只保存一篇，避免多标签页任务串单或取消错任务。
- 取消信号覆盖文件生成、下载交接和浏览器下载阶段；取消与完成使用互斥终态。
- 增加下载状态主动对账与两分钟超时，避免完成事件丢失后界面停在 96%。
- 无图片文章不再申请图片域名权限。
- 音视频和互动组件会明确进入“部分保存”，不再显示完整成功。
- 已完成任务会自动清理，减少长时间运行后的内存占用。

## 0.1.0 · 2026-08-20

- 建立 Chrome / Edge 共用的 Manifest V3 测试版。
- 使用 `activeTab + scripting`，仅在用户点击后读取当前文章。
- 本地提取正文、严格清洗 HTML、内嵌允许范围内的原图。
- 支持完整成功、部分图片失败、失败和取消状态。
- 使用离屏文档继续任务；关闭弹窗不应中断正在进行的保存。
- 浏览器下载完成后才显示成功；重复文件使用 `uniquify`，不覆盖旧文件。
- 没有模型调用、遥测、广告、API Key 或开发者服务器。
